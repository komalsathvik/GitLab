# git merge

## Description

Combines changes from one branch into another.

## Example

`git checkout main`
`git merge feature`

## Merge Conflict

Occurs when Git cannot automatically resolve differences.
