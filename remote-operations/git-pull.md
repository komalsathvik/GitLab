# git pull

## Description

Fetches and merges remote changes into the current branch.

## Command

```bash
git pull origin main
```

**git pull = git fetch + git merge**
Risk

Conflicts may occur if local changes exist.
