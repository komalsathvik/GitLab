# git init

## What is `git init`?

`git init` creates a new Git repository in the current directory.

Internally, Git creates a hidden folder called `.git`.
This folder stores:

- Commit history
- Branch information
- Configuration data

## Command

```bash
git init
```
